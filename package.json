{
  "name": "stl-painter-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser STL painting tool — load an .stl, paint and region-fill colors, export to a color-capable format for multi-material 3D printing.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "lil-gui": "^0.21.0",
    "three": "^0.160.0",
    "three-mesh-bvh": "^0.9.10"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
